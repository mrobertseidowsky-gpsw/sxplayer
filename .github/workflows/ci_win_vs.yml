name: 'tests Windows VS'

on:
  push:
    branches:
      - 'master'
  pull_request:

jobs:
  windows:

    runs-on: windows-latest

    steps: 
    - uses: actions/checkout@v2 
 
    - name: install vcpkg
      shell: cmd
      run: |
        set PKG_CONFIG_PATH="D:\a\sxplayer\sxplayer\vcpkg\installed\x86-windows-static"
        echo %PKG_CONFIG_PATH%
        set VCINSTALLDIR="C:\Program Files (x86)\Microsoft Visual Studio\2019"
        echo %VCINSTALLDIR%
        refreshenv

    - name: install vcpkg
      shell: bash
      run: |      
        choco install meson
        meson setup builddir --backend vs
  
        